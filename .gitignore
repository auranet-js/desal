# Sekrety — NIGDY do repo
.env
.env.*
*.env
!.env.example
*.local
secrets/
*.key
*.pem

# Sekrety per-projekt — placeholderowane w src/, prawdziwe w ~/secrets/desal/

# Mirror MCP server (zawiera token w jawnym tekście — NIE commitować)
kLADte2rC4ckDP8m/

# Backupy MCP — pobierane automatycznie obok plików produkcyjnych
*.bak-*
*.bak-rm-*
backup-*/

# Edytory
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Tmp
tmp/
*.tmp
*.log

# Composer / npm jeśli dojdą
vendor/
node_modules/

# Auranet 2026-05-29: pliki na produkcji z credentials/state (deploy przez MCP/FTP, nie git)
/_sitemap-gen.php
/sitemap.xml
/indexnow-key.txt
